# Changelog

Formato basado en [Keep a Changelog](https://keepachangelog.com/es-ES/1.1.0/).

Este archivo arranca a partir de la versión `0.1.0` (setiembre 2026). Lo anterior — 49 migraciones
y más de 400 commits — está en el historial de git (`git log`), no reconstruido acá.

## [Sin publicar]

### Agregado
- `CLAUDE.md`, `docs/DECISIONES.md` y este `CHANGELOG.md`.
- `.editorconfig`, `.gitattributes` y `.nvmrc`.
- `robots.txt` y meta `noindex` en `index.html`.
- Script `verify` en `package.json`.

### Corregido
- README: la sección de fotos decía que R2 estaba deshabilitado, y está activo en producción.
- README: el comando de secrets apuntaba a Pages (`wrangler pages secret put`) en vez de Workers.

## [0.1.0] — 2026-09-22
Estado del repo al momento de la auditoría contra el manual de arranque de Scalerics.
